# mypka-version

A single version string for a private myPKA scaffold derivative.

`VERSION` is read by `scripts/check-version.py` so an install can tell
whether a newer scaffold release exists. Nothing else lives here.
